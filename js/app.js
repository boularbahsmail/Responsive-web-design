//const card = document.querySelectorAll(".card");
//
//card.forEach((item) =>
//  item.addEventListener("click", () => {
//    card.forEach((element) => (element.className = "card"));
//    if (item.className == "card active") item.className = "card";
//    else item.className = "card active";
//  })
//);
